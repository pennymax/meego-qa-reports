<div class="page_content">

	
	<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
		<h1>Login to MeeGo QA Reports</h1>
		
	  <div class="field last">
	  	<%= f.label :email %>
	  	<%= f.text_field :email, :class => "text short" %>
	  </div>
	
	  <div class="field last">
	  	<%= f.label :password %>
	  	<%= f.password_field :password, :class => "text short" %>
	  </div>
	  
	  
	  <div class="field last no_label">
	  <% if devise_mapping.rememberable? -%>
	    <%= f.check_box :remember_me %> <%= f.label :remember_me_for_2_weeks, :class => "inline_label small" %>
	  <% end -%>
	  </div>
		<div id="wizard_actions">
	  <%= f.submit "Login", :class =>"big_btn login" %>
	  </div>
	<% end %>
	
	<%= render :partial => "devise/shared/links" %>
</div>